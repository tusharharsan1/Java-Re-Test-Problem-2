package com.downloader.controller;

import com.downloader.model.DownloadJob;
import com.downloader.model.DownloadQueue;
import com.downloader.service.DownloadManager;

// TODO 1: Annotate as a Spring REST controller.
public class DownloadController {

    // TODO 2: DownloadManager field, injected via CONSTRUCTOR.

    // TODO 3: runAll(DownloadQueue<DownloadJob> queue) -> delegate (one line).
    // TODO 4: allJobsWithinSizeLimit(DownloadQueue<DownloadJob> queue, long maxBytes) -> delegate (one line).
}
