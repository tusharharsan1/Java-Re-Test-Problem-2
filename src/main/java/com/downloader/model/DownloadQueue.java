package com.downloader.model;

import java.util.List;

public class DownloadQueue<T> {

    // TODO 1: Final field: List<T> items.

    // TODO 2: Constructor(List<T> items) -> store it.

    // TODO 3: public int size() -> return number of items.

    // TODO 4: public List<T> getItems() -> return the list.

    // TODO 5: public List<List<T>> split(int parts)
    //         - Divide items into `parts` roughly-equal sub-lists.
    //         - If parts > size(), some sublists may be empty — do NOT throw.
    //         - No item may be lost; combined size across all sublists must equal size().
    //         (Hint: List.subList(from, to).)
}
